name: Build and Publish etcd x86 Images

on:
  push:
    branches: [ feature/arm ]

  workflow_dispatch:

jobs:
  etcd:
    uses: Surveily/bitnami-docker-etcd/.github/workflows/publish.base.yml@feature/arm
    with:
      arch: "arm64"
      dockerfile: "Dockerfile.arm64"
    secrets:
      registry: ${{ secrets.REGISTRY_DOCKER }}